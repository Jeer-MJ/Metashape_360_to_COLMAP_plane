# Core dependencies (required)
numpy>=1.24
Pillow>=10.0
opencv-python>=4.8

# Optional: 3D point cloud support (PLY loading/writing)
open3d>=0.17

# Optional: AI masking (YOLO segmentation and SAM3)
ultralytics>=8.3
timm>=0.9

# Optional: PLY fallback if open3d is not installed
 plyfile>=0.8
